# PRD — 오늘의 대기질 (가칭)

> 대한민국 17개 시도의 실시간 미세먼지, 측정소별 24시간 추이, 3일 예보를 칩 하나로 확인하는 서비스

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.3 |
| 작성일 | 2026-08-19 |
| 상태 | 초안 — Claude Code 전달 전 |
| 관련 문서 | DESIGN.md (디자인 토큰·컬러·타이포 단일 출처) |

---

## 1. 개요

### 1.1 한 줄 정의
도시 칩을 누르면 그 지역의 현재 대기질, 우리 동네 측정소의 24시간 흐름, 3일 예보, 그리고 오늘 무엇을 하면 되는지를 알려주는 웹 + 크롬 확장.

### 1.2 배경
미세먼지 정보는 이미 넘친다. 문제는 대부분이 **숫자 하나만 던지고 끝난다**는 것이다. 사용자가 실제로 알고 싶은 것은 "PM2.5가 42"가 아니라 "지금 오르는 중인가 내려가는 중인가", "내일은 나아지는가", "창문을 열어도 되는가"다. 이 서비스는 숫자와 행동 사이의 번역을 담당한다.

### 1.3 목표

| 구분 | 목표 |
|---|---|
| 사용자 | 3초 안에 지역 대기질과 행동 지침을 알고, 원하면 우리 동네 흐름까지 파고든다 |
| 제품 | 클릭 1회로 완결되되, 깊이는 선택적으로 열린다 |
| 학습 | 공공데이터포털 API 3종, 서버리스 함수, 크롬 확장, Gemini 연동을 한 프로젝트에서 관통 |

### 1.4 정보 계층 원칙

이 서비스에는 **성격이 다른 두 숫자**가 한 화면에 놓인다.

| 영역 | 기준 | 역할 |
|---|---|---|
| 메인 카드 | 시도 전체 측정소 **평균** | 지역 요약 |
| 추이 그래프 | 사용자가 선택한 **측정소 1곳** | 우리 동네 상세 |

두 값은 일치하지 않는다. 이것은 오류가 아니라 **계층**이다. 각 영역에 기준을 명시해 사용자가 혼동하지 않도록 한다. 카드에 "경기 평균 45", 그래프에 "수원 측정소 38"이 동시에 있는 것이 정상이다.

### 1.5 비목표 (Out of Scope)

| 항목 | 사유 |
|---|---|
| **시도 평균의 24시간 추이** | 시간별 이력은 측정소 단위로만 제공된다. 시도 평균의 추이를 그리려면 측정소 수십~백여 개를 각각 호출해야 해 일일 한도를 초과한다. 추이는 **측정소 단위로만** 제공한다 |
| 사용자 계정, 즐겨찾기 서버 동기화 | — |
| 알림·푸시 | — |
| 크롬 웹스토어 정식 등록 | 개발자 등록 5달러 필요. 비공개 설치까지만 |

---

## 2. 사용자와 시나리오

### 2.1 주 사용자
아침에 창문을 열지 말지, 아이를 밖에서 놀릴지, 러닝을 나갈지, 주말 약속을 실내로 옮길지 판단해야 하는 사람.

### 2.2 핵심 시나리오

**S1. 웹 — 아침 확인 (얕게)**
사이트에 들어온다 → 지난번에 본 도시가 이미 선택되어 데이터가 떠 있다 → 현재 수치와 "환기 / 마스크 / 운동" 세 줄을 읽는다 → 끝.

**S2. 웹 — 우리 동네 파고들기 (깊게)**
측정소 드롭다운에서 집 근처 측정소를 고른다 → 24시간 추이를 본다 → 새벽에 치솟았다가 지금 내려가는 중임을 확인한다.

**S3. 웹 — 주말 계획**
3일 예보에서 모레 등급을 확인하고 야외 활동 여부를 정한다.

**S4. 확장 — 툴바에서 즉시 확인**
툴바 아이콘을 누른다 → 팝업이 열리는 즉시 마지막에 본 도시 데이터가 보인다 → 확인하고 닫는다.

> **설계 원칙**: 팝업은 여는 순간 답이 나와야 한다. 열고 나서 칩을 또 고르거나 스크롤해야 한다면 확장으로 만든 의미가 절반 사라진다.

---

## 3. 정보 구조 및 화면 명세

### 3.1 화면 구성

```
┌─────────────────────────────────┐
│ 헤더    서비스명 · 새로고침 버튼   │
├─────────────────────────────────┤
│ 칩 영역  17개 시도 (flex-wrap)   │
├─────────────────────────────────┤
│ 메인 카드  — 지금 (시도 평균)     │
│  · 도시명                        │
│  · PM10 · PM2.5 큰 숫자 + 등급   │
│  · 가장 나쁜 측정소 한 줄         │
│  · 유효 측정소 수 · 측정 시각     │
├─────────────────────────────────┤
│ 측정소 상세  (측정소 선택)        │
│  · 드롭다운                      │
│  · 24시간 추이 그래프             │
├─────────────────────────────────┤
│ 3일 예보  오늘 · 내일 · 모레      │
├─────────────────────────────────┤
│ 조언 영역  환기 / 마스크 / 운동   │
├─────────────────────────────────┤
│ 더보기 (접힘)  O3·NO2·CO·SO2·CAI │
└─────────────────────────────────┘
```

**배치 근거**: 시선이 "지금 → 우리 동네 흐름 → 앞으로 3일 → 그래서 뭘 하지" 순으로 흐른다. 조언은 앞의 모든 정보를 근거로 삼으므로 마지막에 온다.

### 3.2 칩 영역

| 항목 | 명세 |
|---|---|
| 개수 | 17개 고정 (API `sidoName` 허용값과 1:1) |
| 목록 | 서울, 부산, 대구, 인천, 광주, 대전, 울산, 세종, 경기, 강원, 충북, 충남, 전북, 전남, 경북, 경남, 제주 |
| 레이아웃 | `flex-wrap`으로 전체 노출. 가로 스크롤 사용 안 함 |
| 선택 상태 | 배경·테두리·굵기 **3중 표현**. 색상 단독 구분 금지 |
| 접근성 | `role="button"` + `aria-pressed`, 키보드 Tab 탐색 가능 |

### 3.3 메인 카드 (시도 평균)

| 요소 | 내용 | 비고 |
|---|---|---|
| 도시명 | 선택된 시도명 | |
| 기준 라벨 | "시도 평균" 명시 | §1.4 원칙 |
| PM10 | 평균값 + 단위(㎍/㎥) + 등급 배지 | 주 정보 |
| PM2.5 | 평균값 + 단위 + 등급 배지 | 주 정보 |
| 최악 측정소 | "가장 나쁜 곳: ○○ 측정소 120" | 보조 1줄 |
| 데이터 신뢰 표기 | "37개 측정소 중 34개 기준" | 결측 투명 공개 |
| 측정 시각 | `dataTime` 값을 한국식 표기로 | 필수 |

**최악 측정소를 함께 보여주는 이유**
평균만 쓰면 국지적 고농도가 지워진다. 경기도처럼 넓은 시도는 평균 40인데 특정 측정소만 120인 상황이 실제로 발생하고, 그 지역 사용자에게 "보통"이라고 말하는 것은 서비스가 거짓말을 하는 것이다. 반대로 최악값만 표시하면 매일 "나쁨"이 떠서 사용자가 곧 무시하게 된다. **평균을 주 숫자로, 최악을 보조 한 줄로** 두어 사용자가 스스로 판단할 재료를 준다.

### 3.4 측정소 상세 — 드롭다운 + 24시간 추이

**측정소 드롭다운**

| 항목 | 명세 |
|---|---|
| 목록 출처 | **실시간 응답의 `stationName` 배열을 그대로 사용. 추가 호출 0** |
| 기본 선택 | 메인 카드의 **최악 측정소** |
| 정렬 | 현재 농도 내림차순 |
| 변경 시 | 해당 측정소의 24시간 추이만 재호출 |

> 기본값을 최악 측정소로 두면 카드의 "가장 나쁜 곳: ○○ 측정소"에서 그래프로 시선이 자연스럽게 이어진다. 사용자가 "우리 동네가 저긴가?"를 확인하는 동선이 그대로 기능이 된다.

**24시간 추이 그래프**

| 항목 | 명세 |
|---|---|
| 구현 | Chart.js CDN |
| 데이터 | 선택 측정소의 최근 24시간 시간별 값 |
| 계열 | PM10, PM2.5 2개 라인 |
| 라벨 | 그래프 제목에 **"○○ 측정소 기준"** 반드시 명시 |
| 시간 축 | 오래된 값이 왼쪽. **응답이 최신순이므로 역순 정렬 필요** |
| 결측 처리 | `-` 값은 `null`로 변환. 선을 잇지 않고 끊는다 (`spanGaps: false`) |
| 확장 팝업 | **기본 접힘** (§6.5 높이 예산) |

> 결측을 0으로 찍으면 그래프가 바닥으로 내리꽂혀 "공기가 갑자기 깨끗해졌다"로 읽힌다. 반드시 끊어야 한다.

### 3.5 3일 예보

오늘 · 내일 · 모레 3칸 가로 배치. 각 칸에 날짜, PM10 등급, PM2.5 등급.

| 요소 | 명세 |
|---|---|
| 표시 항목 | 날짜 라벨 + PM10 등급 + PM2.5 등급 |
| 등급 표현 | 메인 카드와 동일한 4단계 배지. 색상 단독 구분 금지 |
| 발생 원인 텍스트 | **화면 미노출.** Gemini 입력으로만 사용 (§5.3) |
| 예보 미발표 시 | 해당 칸을 "예보 준비 중"으로 표시. 칸 자체를 숨기지 않음 |

**경기·강원의 이중 권역 처리**
예보는 시도가 아닌 권역 단위로 발표되며, 경기는 북부/남부, 강원은 영서/영동으로 나뉜다(§4.7).

- 등급 배지는 **더 나쁜 쪽**을 대표값으로 표시한다
- 그 아래 작은 글씨로 "북부 나쁨 · 남부 보통"처럼 병기한다
- 메인 카드의 "최악 측정소 병기"와 같은 원칙이다. 나쁜 쪽을 숨기지 않되, 세부를 함께 보여준다

### 3.6 더보기 영역
기본 접힘. 펼치면 O3, NO2, CO, SO2, 통합대기환경지수(CAI) 표시. 기준은 **선택된 측정소**.

> 이 값들은 실시간 응답에 **이미 딸려 오므로 추가 호출 비용이 0**이다.

---

## 4. 데이터 명세

### 4.1 사용 API 개요

세 오퍼레이션 모두 **동일한 서비스(`ArpltnInforInqireSvc`)에 속한다. 활용신청 1회, 인증키 1개로 전부 호출된다.**

| 용도 | 오퍼레이션 |
|---|---|
| 시도 현재 수치 + 측정소 목록 | `getCtprvnRltmMesureDnsty` |
| 측정소 24시간 추이 | `getMsrstnAcctoRltmMesureDnsty` |
| 3일 예보 | `getMinuDustFrcstDspth` |

### 4.2 시도별 실시간 측정정보 조회

```
GET https://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getCtprvnRltmMesureDnsty
```

| 파라미터 | 값 | 비고 |
|---|---|---|
| `serviceKey` | 일반 인증키 | 환경변수에서 로드 |
| `returnType` | `json` | |
| `numOfRows` | `200` | 경기도 측정소 수가 가장 많음. 여유 확보 |
| `pageNo` | `1` | |
| `sidoName` | 선택된 시도명 | |
| `ver` | `1.5` | **개발 착수 전 검증 필요 (§4.8)** |

**이 응답 하나가 두 가지 역할을 한다**
① 시도 평균·최악 산출 (메인 카드) ② 측정소 드롭다운 목록 (§3.4)

### 4.3 응답 처리 규칙 (실시간)

1. 각 측정소의 `pm10Value`, `pm25Value`를 순회한다
2. 값이 `-`, 빈 문자열, `null`인 측정소는 **계산에서 완전히 제외한다**
3. 남은 유효 측정소의 산술평균을 소수점 없이 반올림하여 주 숫자로 사용한다
4. 유효 측정소 중 최댓값과 그 `stationName`을 최악 측정소로 저장한다 (드롭다운 기본값)
5. 전체 측정소 수와 유효 측정소 수를 함께 반환한다
6. 유효 측정소가 0개이면 해당 항목은 "측정 불가"로 표시한다 (0으로 표시 금지)

> **주의**: 점검 중이거나 통신 장애인 측정소는 값을 `-` 또는 빈 문자열로 반환한다. 이를 숫자로 변환하면 평균이 통째로 망가진다. 이 규칙은 협상 대상이 아니다.

### 4.4 측정소별 실시간 측정정보 조회 (24시간 추이)

```
GET https://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getMsrstnAcctoRltmMesureDnsty
```

| 파라미터 | 값 | 비고 |
|---|---|---|
| `serviceKey` | 동일한 키 | |
| `returnType` | `json` | |
| `numOfRows` | `24` | 24시간분 |
| `pageNo` | `1` | |
| `stationName` | 선택된 측정소명 | 드롭다운 값 |
| `dataTerm` | `DAILY` | `DAILY` / `MONTH` / `3MONTH` 중 |
| `ver` | `1.5` | |

**응답 처리**
1. 시간별 item 배열이 반환된다. 각 item에 `dataTime`, `pm10Value`, `pm25Value` 포함
2. **역순 정렬한다** — 응답은 최신순이나 그래프는 오래된 값이 왼쪽이어야 한다
3. `-`·빈 문자열은 `null`로 변환한다 (0 아님)
4. `dataTime`을 시각 라벨(HH시)로 가공한다

### 4.5 등급 기준 — 환경부 4단계

**PM10 (㎍/㎥)**

| 등급 | 범위 |
|---|---|
| 좋음 | 0 ~ 30 |
| 보통 | 31 ~ 80 |
| 나쁨 | 81 ~ 150 |
| 매우나쁨 | 151 이상 |

**PM2.5 (㎍/㎥)**

| 등급 | 범위 |
|---|---|
| 좋음 | 0 ~ 15 |
| 보통 | 16 ~ 35 |
| 나쁨 | 36 ~ 75 |
| 매우나쁨 | 76 이상 |

- 등급은 **평균값을 기준으로 자체 계산**한다 (응답의 `pm10Grade`는 개별 측정소 값이므로 평균에 그대로 쓸 수 없다)
- 예보 응답의 등급은 **이미 문자열로 제공**되므로 자체 계산하지 않고 그대로 사용한다
- 그래프 배경에 등급 구간 밴드를 넣을 경우 위 경계값을 사용한다
- 등급 표현은 **색 + 텍스트 라벨 + 아이콘 3중**으로. 색상 단독 구분 금지 (색각 이상 대응)

### 4.6 갱신 정책

| 동작 | 호출 |
|---|---|
| 최초 로드 | 실시간 1 + 추이 1 + 예보 (캐시 히트 시 0) |
| 칩 전환 | 실시간 1 + 추이 1. **예보는 재호출하지 않는다** (전국 응답 보유) |
| 측정소 드롭다운 변경 | 추이 1만 |
| 수동 새로고침 | 실시간 1 + 추이 1 |
| 자동 폴링 | **사용하지 않음** |

**서버 캐시**

| 대상 | 헤더 |
|---|---|
| 실시간 | `Cache-Control: s-maxage=600, stale-while-revalidate=1200` |
| 추이 | `Cache-Control: s-maxage=600, stale-while-revalidate=1200` |
| 예보 | `Cache-Control: s-maxage=21600, stale-while-revalidate=43200` (6시간) |

### 4.7 대기질 예보통보 조회

```
GET https://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getMinuDustFrcstDspth
```

| 파라미터 | 값 | 비고 |
|---|---|---|
| `serviceKey` | 동일한 키 | |
| `returnType` | `json` | |
| `numOfRows` | `100` | |
| `pageNo` | `1` | |
| `searchDate` | `YYYY-MM-DD` (통보 발령일) | 예보 대상일이 아님에 주의 |
| `InformCode` | `PM10` 또는 `PM25` | **대문자 I로 시작. §4.8 검증 대상** |

**호출 구조**
`InformCode`별로 1회씩, 총 2회 호출하면 **전국 모든 권역의 3일치 예보**를 확보한다. 시도별 반복 호출이 필요 없다.

**응답 처리**

| 필드 | 용도 |
|---|---|
| `informData` | 예보 대상일. 오늘/내일/모레 구분 |
| `informGrade` | 권역별 등급 문자열. 파싱 필요 |
| `informCause` | 발생 원인 텍스트. **화면 미노출, Gemini 입력용** |
| `informOverall` | 예보 개요 텍스트. **화면 미노출, Gemini 입력용** |
| `dataTime` | 통보 시각. 같은 날 여러 건 중 최신 선택 |

1. 같은 `searchDate`에 여러 통보가 반환된다(하루 4회 발표). `dataTime`이 최신인 건을 채택한다
2. `informData` 값으로 오늘/내일/모레 3건을 분류한다
3. `informGrade`는 `"서울 : 보통,경기북부 : 나쁨,강원영서 : 보통,..."` 형태의 단일 문자열이다. 쉼표로 분리 후 콜론으로 나누어 권역별 등급 맵을 만든다

**권역 → 시도 매핑**

| 시도 | 예보 권역 |
|---|---|
| 경기 | 경기북부, 경기남부 |
| 강원 | 강원영서, 강원영동 |
| 그 외 15개 시도 | 동일 명칭 1:1 |

### 4.8 개발 착수 전 검증 항목

공공데이터포털 마이페이지 → 개발계정 → 미리보기에서 다음을 **코드 작성 전에 눈으로 확인한다.**

**시도별 실시간**
`ver=1.5`로 요청하여 필드 존재 확인: `pm10Value`, `pm25Value`, `khaiValue`, `o3Value`, `no2Value`, `coValue`, `so2Value`, `dataTime`, `stationName`

> `ver` 값에 따라 응답 필드 구성이 달라진다. 낮은 버전에서는 PM2.5 자체가 오지 않는다.

**측정소별 시간별**
- `dataTerm=DAILY`로 반환되는 item 개수 (24개인지, 그 이상인지)
- 정렬 순서 (최신순인지 오래된 순인지)
- 결측 시간대의 실제 표기

**예보**
- `InformCode` 파라미터의 정확한 대소문자 표기
- `informGrade` 문자열의 실제 구분자와 권역 명칭 (문서와 실제 응답이 다를 수 있음)
- 오늘 날짜로 조회했을 때 반환 건수

---

## 5. AI 조언 기능 (Gemini)

### 5.1 무엇을 시키고 무엇을 시키지 않는가

**시키지 않는 것 — 정적 테이블로 처리**
"나쁨이면 마스크를 쓰세요" 수준의 등급 → 문구 1:1 매핑. AI 없이 충분하며, AI에게 시키면 비용만 쓰는 껍데기가 된다.

**시키는 것 — 조건이 엇갈리는 상황의 한국어 서술**
- PM10은 보통인데 PM2.5만 나쁨인 경우
- 평균과 최악 측정소의 격차가 큰 경우
- **최근 몇 시간 추세가 상승 중인 경우** (또는 하강 중)
- 현재는 보통인데 내일 예보가 나쁨인 경우 (또는 반대)
- 오존 등 다른 항목이 함께 높은 경우
- 측정 시각이 오래된 경우

이런 조합은 경우의 수가 많아 정적 테이블로 감당되지 않는다. **Gemini는 여기서만 값을 더한다.**

### 5.2 명세

| 항목 | 내용 |
|---|---|
| 모델 | `gemini-3.0-flash` |
| 출력 형태 | JSON 고정 — `{ ventilation, mask, exercise }` |
| 각 필드 | 한국어 1문장, 40자 이내 |
| 호출 시점 | 실시간 + 추이 + 예보 수신 후 |
| 실패 처리 | **정적 문구 테이블로 폴백. 화면은 절대 비우지 않는다** |

> 폴백은 선택 사항이 아니다. 발표 중 무료 한도 초과로 조언 영역이 비는 사고를 막는 장치다.

### 5.3 입력으로 넘길 데이터

| 구분 | 항목 |
|---|---|
| 현재 | 시도명, PM10 평균·등급, PM2.5 평균·등급, 최악 측정소 값, 유효/전체 측정소 수, 측정 시각 |
| 추이 | 선택 측정소명, 최근 6시간 PM2.5 값 배열 (24개 전부 넘기지 않음) |
| 예보 | 오늘·내일·모레 PM10/PM2.5 등급, `informOverall`, `informCause` |

**개인정보는 일절 포함하지 않는다.**

> 추이는 최근 6시간만 넘긴다. 24개 전부는 토큰만 늘리고 "오르는 중/내려가는 중" 판단에는 6개면 충분하다.

> `informCause`와 `informOverall`은 화면에 노출하지 않지만 Gemini에는 넘긴다. 같은 응답에 이미 포함되어 추가 호출 비용이 0이면서, 조언 문장에 근거를 부여한다. "내일은 마스크를 챙기세요"와 "내일은 대기 정체로 농도가 오를 전망이니 마스크를 챙기세요"의 차이다.

---

## 6. 아키텍처

### 6.1 웹 — 서버 경유 필수

```
브라우저 → /api/air       → 에어코리아 (시도 실시간)
브라우저 → /api/station   → 에어코리아 (측정소 24시간)
브라우저 → /api/forecast  → 에어코리아 (예보)
브라우저 → /api/advice    → Gemini
```

공공데이터포털은 CORS를 차단하고 인증키를 요구한다. 브라우저에서 직접 호출할 수 없으며, 키를 프론트엔드에 두는 것은 불가능하다.

### 6.2 확장 — 서버 선택적

```
팝업 → 에어코리아 (실시간·추이·예보)  직접 호출, 사용자 키
팝업 → /api/advice                   내 서버 경유
```

확장은 `host_permissions`에 주소를 등록하면 CORS 검사를 통과한다. 막는 주체가 브라우저인데, 확장은 브라우저가 발급한 통행증을 들고 있는 셈이다.

**왜 조언만 서버를 경유하는가**
대기질 데이터를 직접 호출하기로 한 이상 에어코리아 키는 사용자가 입력해야 한다. 여기에 Gemini까지 직접 호출하면 키 입력창이 두 개가 되고, 확장을 켜자마자 설정 화면부터 만나게 된다. 조언은 호출량이 적고 개인정보가 없으므로 내 서버가 부담하는 편이 낫다.

### 6.3 폴더 구조

```
air-quality/
├─ index.html          화면. 누구나 볼 수 있음
├─ api/
│  ├─ air.js           시도 실시간 + 대표값 가공
│  ├─ station.js       측정소 24시간 + 역순 정렬 + 결측 null화
│  ├─ forecast.js      예보 + 권역 파싱
│  └─ advice.js        Gemini + 폴백
├─ .env.local          키 보관. 내 컴퓨터에만 존재
├─ .gitignore          .env.local 차단
└─ extension/
   ├─ manifest.json
   ├─ popup.html
   ├─ popup.js
   ├─ icon48.png
   └─ icon128.png
```

### 6.4 키 보관 3원칙

| 위치 | 처리 |
|---|---|
| 내 컴퓨터 | `.env.local`에 기록 |
| GitHub | **올리지 않음** — `.gitignore`에 `.env.local` |
| Vercel | 대시보드 환경변수에 별도 등록 |

> 로컬은 되는데 배포만 안 된다면 거의 100% 세 번째를 빠뜨린 것이다.

### 6.5 확장 manifest 및 높이 예산

```json
{
  "manifest_version": 3,
  "name": "오늘의 대기질",
  "version": "1.0.0",
  "description": "툴바에서 바로 보는 실시간 대기질",
  "icons": { "48": "icon48.png", "128": "icon128.png" },
  "action": { "default_popup": "popup.html" },
  "permissions": ["storage"],
  "host_permissions": [
    "https://apis.data.go.kr/*",
    "https://<배포도메인>/*"
  ]
}
```

**팝업 높이 예산 (크롬 최대 600px)**

| 영역 | 높이 | 확장에서 |
|---|---|---|
| 칩 4줄 | 140px | 표시 |
| 메인 카드 | 180px | 표시 |
| 측정소 선택 | 40px | 표시 |
| 24시간 추이 | 160px | **기본 접힘** |
| 3일 예보 | 90px | 표시 |
| 조언 | 90px | 표시 |
| 펼친 합계 | 700px | 초과 |
| **접은 합계** | **약 540px** | 수용 |

> 전부 펼치면 700px로 한도를 넘어 스크롤이 생긴다. 팝업에서 스크롤은 "열자마자 답이 보인다"는 원칙을 깬다. 확장에서는 **추이 그래프를 기본 접힘**으로 두고, 웹에서는 펼친 상태를 유지한다.

### 6.6 상태 저장

| 대상 | 웹 | 확장 |
|---|---|---|
| 마지막 선택 도시 | `localStorage` | `chrome.storage.local` |
| 사용자 API 키 | 해당 없음 | `chrome.storage.local` |

> 저장 로직은 **`getLastCity()` / `setLastCity()` 형태의 얇은 함수 2개로 분리**한다. 이식할 때 이 함수 내부만 교체하면 되고, 나머지 코드는 손대지 않는다.

---

## 7. 상태별 화면

| 상태 | 화면 |
|---|---|
| 로딩 | 카드·그래프 형태를 유지한 스켈레톤. 레이아웃 흔들림 없음 |
| 성공 | §3.3 ~ §3.5 |
| 부분 결측 (측정소) | 정상 표시 + "37개 중 34개 기준" 문구 |
| 부분 결측 (시간) | 그래프에서 해당 구간 선 끊김. 0으로 찍지 않음 |
| 전체 결측 | "해당 지역 측정값이 일시적으로 없습니다" + 새로고침 버튼 |
| 추이 조회 실패 | 그래프 영역만 "추이를 불러오지 못했습니다" + 재시도. **카드·예보·조언은 정상** |
| 예보 일부 미발표 | 해당 칸만 "예보 준비 중". 나머지 칸은 정상 |
| 예보 전체 실패 | 예보 영역만 숨김. **나머지는 정상 동작** |
| 실시간 API 오류 | "데이터를 불러오지 못했습니다" + 재시도 버튼 |
| 조언 실패 | 정적 문구로 대체. **오류 표시 없음** |
| 확장 — 키 미입력 | 키 입력 안내 화면 + 발급 링크 |

> **영역별 독립 실패 처리**를 적용한다. 하나의 실패가 전체 화면을 죽이지 않는다. 단, 시도 실시간 조회는 다른 모든 영역의 전제이므로 예외다.

---

## 8. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 반응형 | 모바일 우선. 360px에서 깨지지 않음 |
| 접근성 | 등급을 색상 단독으로 구분하지 않음. 칩·드롭다운 키보드 탐색 가능. 그래프 하단에 수치 요약 텍스트 병기 |
| 성능 | 진입 후 첫 데이터 표시까지 2초 이내. 그래프는 이후 지연 렌더 허용 |
| 디자인 | DESIGN.md 단일 출처 준수. 본 문서에 토큰 값 미기재 |
| 스타일링 | Tailwind CDN |
| 차트 | Chart.js CDN. 색상은 DESIGN.md 토큰 사용 |

---

## 9. 호출 예산

**개발계정 일일 트래픽: 500회**

| 용도 | 호출 수 |
|---|---|
| 예보 | 2회 × 4회/일 = **8회** (전국 공통, 6시간 캐시) |
| 시도 실시간 | 칩 전환당 1회 |
| 측정소 추이 | 칩 전환당 1회 + 드롭다운 변경당 1회 |
| 잔여 | 약 492회 |

**칩 전환 1회 = 2호출.** 캐시 없이 계산하면 약 246회 전환이 한계이며, 드롭다운을 자주 바꾸면 더 빨리 소진된다.

서버 캐시(10분)를 적용하면 같은 시도·측정소를 반복 조회해도 원본 호출이 발생하지 않는다. **혼자 사용하고 발표하는 데는 충분하지만, 캐시 없이 여러 명이 동시에 접근하면 순식간에 소진된다.** 캐싱은 선택이 아니라 필수다.

---

## 10. 리스크

| # | 리스크 | 대응 |
|---|---|---|
| R1 | `serviceKey` 이중 인코딩으로 인증 실패 | **가장 흔한 실패 지점.** 디코딩 키 사용 + 인코딩 1회만. `URLSearchParams` 사용 시 이중 인코딩 주의 |
| R2 | 일일 호출 한도(500회) 초과 | §9 예산 준수. 서버 캐시 필수 |
| R3 | `ver` 값에 따라 PM2.5 필드 부재 | §4.8 사전 검증 필수 |
| R4 | 결측값이 평균을 오염 | §4.3 규칙 엄수 |
| R5 | **그래프 시간축 역순** | 응답이 최신순으로 오므로 정렬하지 않으면 시간이 거꾸로 흐른다. §4.4 규칙 엄수 |
| R6 | **결측을 0으로 찍어 그래프가 바닥으로 꺾임** | `null` 변환 + `spanGaps: false` |
| R7 | 새벽 시간대 당일 예보 미발표 | `searchDate`를 오늘로 조회해 결과가 없으면 **전날로 재조회하는 폴백** 구현 |
| R8 | 예보 권역명이 문서와 실제 응답에서 상이 | §4.8에서 실제 응답 문자열 확인 후 매핑 테이블 확정 |
| R9 | 확장 팝업 높이 초과 | 추이 그래프 기본 접힘 (§6.5) |
| R10 | Gemini 무료 한도 초과 | 정적 문구 폴백 (§5.2) |
| R11 | 발표 시연 중 API 장애 | 마지막 정상 응답을 캐시하여 시연용 폴백 확보 |

---

## 11. 개발 태스크

### Phase 1 — 웹 (실시간 + 추이 + 예보)
- [ ] 공공데이터포털 활용신청 및 인증키 발급
- [ ] 포털 미리보기로 3개 오퍼레이션 응답 검증 (§4.8)
- [ ] `.env.local` + `.gitignore` 설정 → **커밋 전 반드시 확인**
- [ ] `api/air.js` — 호출, 결측 제외, 평균·최악 산출, 등급 계산, 측정소 목록 반환
- [ ] `api/station.js` — 호출, 역순 정렬, 결측 null 변환
- [ ] `api/forecast.js` — 호출, 최신 통보 선택, 권역 파싱, 시도 매핑, 전날 폴백
- [ ] `index.html` — 칩 17개, 메인 카드, 측정소 드롭다운, Chart.js 그래프, 3일 예보, 더보기
- [ ] 영역별 독립 실패 처리 (§7)
- [ ] `localStorage` 마지막 도시 복원
- [ ] 서버 캐시 헤더 적용 (§4.6)
- [ ] Vercel 배포 + 환경변수 등록

### Phase 2 — AI 조언
- [ ] Gemini API 키 발급
- [ ] 정적 폴백 문구 테이블 작성 (**AI보다 먼저**)
- [ ] `api/advice.js` — JSON 출력, 추이·예보 입력, 폴백 처리
- [ ] 조언 영역 UI 연결

### Phase 3 — 확장 이식
- [ ] `extension/` 폴더 구성, `manifest.json` 작성
- [ ] `popup.html` — 웹 마크업 이식, 그래프 기본 접힘 처리
- [ ] `popup.js` — 저장 함수를 `chrome.storage`로 교체
- [ ] 키 입력 화면 구현
- [ ] 팝업 높이 600px 이내 검증
- [ ] 압축해제된 확장으로 로드하여 동작 확인

### 완료 기준
배포된 주소에서 17개 시도의 현재 수치, 측정소별 24시간 추이, 3일 예보가 모두 정상 조회되고, 크롬에 설치한 확장이 툴바에서 같은 결과를 보여준다.

---

## 12. 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v0.1 | 2026-08-19 | 최초 작성 |
| v0.2 | 2026-08-19 | 3일 예보 추가. 24시간 추이 비목표 처리. 호출 예산 신설. 영역별 독립 실패 처리 추가 |
| v0.3 | 2026-08-19 | 24시간 추이 재도입 — 측정소 계층 구조로 해결(§1.4, §3.4, §4.4). Chart.js 확정. 확장 팝업 높이 예산 및 접힘 처리 추가(§6.5) |
